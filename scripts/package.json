{
  "name": "scripts",
  "version": "0.0.1",
  "description": "Scripts to help development",
  "license": "GPL-3.0",
  "private": true,
  "devDependencies": {
    "@types/node": "^14.14.35",
    "@types/which": "^2.0.0",
    "typescript": "^4.2.3"
  },
  "scripts": {
    "build": "yarn node dist/build.js",
    "schema": "npx typescript-json-schema src/build/config.ts BuildConfig -o build.schema.json"
  },
  "dependencies": {
    "commander": "^7.1.0",
    "source-map-support": "^0.5.19",
    "typescript-json-schema": "^0.50.0",
    "which": "^2.0.2"
  }
}
